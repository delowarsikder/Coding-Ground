#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

#define FastIO ios::sync_with_stdio(0);cin.tie(0);

int main(){
    FastIO;
    ll n,x;
    cin>>n>>x;
    vector<ll>arr(n);
    for(ll i=0;i<n;i++){
        cin>>arr[i];
    }
    ll sum=0;
    for(ll i=0;i<n;i++){

    }
return 0;}
