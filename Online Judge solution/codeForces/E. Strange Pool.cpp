#include<bits/stdc++.h>
using namespace std;

#define FastIO ios::sync_with_stdio(0);cin.tie(0);

typedef long long ll;

vector<ll>pool,bottol;
int main()
{
    FastIO;
    ll n,m;
    cin>>n>>m;
    ll t;

    for(ll i=0;i<n;i++){
        cin>>t;
        pool.push_back(t);
    }
    for(ll i=0;i<m;i++){
        cin>>t;
        bottol.push_back(t);
    }

    for(ll i=0;i<n;i++){
        for(ll j=0;j<m;j++){

        }
    }

    return 0;
}
