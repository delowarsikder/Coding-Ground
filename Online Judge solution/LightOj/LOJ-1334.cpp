#include <bits/stdc++.h>
using namespace std;

#define FastIO               \
    ios::sync_with_stdio(0); \
    cin.tie(0);
typedef long long ll;
int main()
{
    FastIO;
    int tc;
    cin >> tc;
    int _case = 0;
    // str= DNA and ptr=Gene

    string str, ptr;
    cin >> str >> ptr;
    int lps[];

    return 0;
}